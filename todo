Algorytm
    Dodaj śledzenie jak są poukładane zmienne w bazie (przy zerowaniu kolumny i wpisywaniu 1)
    (powinny być x2 = 5, x1 = 4)
    Minimum nie działa!
    Dowiedz się jak przekazuje się solver w konstruktorze Model()
    Zrób z tego klasę, fork JuMP'a
    Wyczyść kod, zmień nazwy prywatnych funkcji
Generyczność
    Dorób obsługę większej ilości typów? (nie tylko bin, int, lower_bound, etc)
